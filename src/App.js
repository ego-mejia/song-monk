import "./App.css";
// Este será el componente raíz. Importa y usa los componentes Header y Song.
// Muestra al menos tres canciones en la pantalla usando múltiples instancias del componente Song.

// Estilizar la app
// Aplica estilos básicos con CSS para que la interfaz sea clara
//  y fácil de leer. Puedes usar un archivo App.css para esto.

// Imprimir un mensaje con componentDidMount

// En el componente App, implementa el método componentDidMount
// para mostrar un mensaje en la consola indicando que la app se ha cargado correctamente.

function App() {
  return (
    <div className="App">
      <header className="App-header"></header>
    </div>
  );
}

export default App;
